
1장

2장
2.1

19p
이 책에서는 최신 버전인 Python 3.6 버전을 사용하려고 합니다. ‘Python 3.6.5’를 클릭하여 exe 파
일을 다운로드하여 설치를 진행합니다. 맥이라면 .dmg로 내려받아집니다

-->

이 책에서는 파이썬 3.6을 사용 했습니다. 하지만 파이썬은 버젼이 올라가는 속도가 빠르기 때문에 현재 올라와있는 3.x버젼을 사용하시면 됩니다.

 ‘Python 3.x.x’를 클릭하여 exe 파
일을 다운로드하여 설치를 진행합니다. 맥이라면 .dmg로 내려받아집니다.

-------------------------------------------

윈도우 버전 파이썬 인스톨러를 실행시키면 그림과 같은 화면이 보입니다. ‘Add Python 3.6 to
PATH’를 선택할 경우 운영체제 환경 변수에 파이썬 경로를 잡아줘서 어떤 디렉토리에서도 파이썬
을 실행시킬 수 있도록 합니다. 

=>

윈도우에서 파이썬을 설치하면 아래 화면과 같이 진행 됩니다.  ‘Add Python 3.x to PATH’를 선택할 경우 운영체제 환경 변수에 파이썬 경로를 잡아줘서 어떤 디렉토리에서도 파이썬
을 실행시킬 수 있도록 합니다.


---------------------------------------------
20p
윈도우에서 Python 3.6.5 버전 설치를 완료하였습니다. 맥에서의 설치 크게 다르지 않으며, 맥은 파
이썬을 기본으로 제공하여 터미널(Terminal) 앱에서 버전을 확인(python --version)해보고 설치를
결정하는 것도 좋습니다.

=>

윈도우에서 Python 3.x.x 버전 설치를 완료하였습니다. 맥에서의 설치 크게 다르지 않으며, 맥은 파
이썬을 기본으로 제공하여 터미널(Terminal) 앱에서 버전을 확인(python --version)해보고 설치를
결정하는 것도 좋습니다.

22p
또는 파이참 공식 홈페이지 https://www.jetbrains.com/pycharm/에 접속하여, 우측 상
단 배너의 [Download]를 클릭하면 다운로드 페이지로 들어갈 수 있습니다.

=> 



23p
이 책에서는 무료인 ‘커뮤니티 버전’으로 진행합니다. ‘프로페셔널 버전’은 개발자가 개발할때 조금 더 편한 기능을 제공하지만 이 책의 예제들은 무료 버전인 커뮤니티 버전으로도 충분합니다. 이런 종류의 툴 중에는 학생 인증을 통해서 무료 학생 라이센스로 사용할 수 있도록 해주기 때문에 학생이라면 이 방법을 이용하는게 좋습니다. 
=>

이 책에서는 무료인 ‘커뮤니티(Community)  버전’으로 진행합니다. ‘프로페셔널(Professional) 버전’은 개발자가 개발할때 조금 더 편한 기능을 제공하지만 이 책의 예제들은 무료 버전인 커뮤니티 버전으로도 충분합니다. 이런 종류의 툴 중에는 학생 인증을 통해서 무료 학생 라이센스로 사용할 수 있도록 해주기 때문에 학생이라면 이 방법을 이용하는게 좋습니다. 


---------------------------


## 5장
98p
[그림 5-4] 빼주세요. [그림 5-3]과 중복 됩니다.


99~100p
웹 브라우저는 텍스트 형태로 되어 있는 HTML문서를 읽어서 우리가 보기 좋게 그려 주는(rendering)기능을 하는 프로그램입니다.

=>
웹 브라우저는 텍스트 형태로 되어 있는 HTML문서를 읽어서 우리가 보기 좋게 그려 주는 렌더링(rendering)기능을 하는 프로그램입니다.
-----------------------------------------

100p
파이참을 실행해봅니다.
[01_naver_crawl.py파일 만들기 스샷] 첨부 할 것

=>
파이참을 실행해봅니다.
-----------------------------------------

102p
HTML은 위 그림 처럼 HTML 소스코드(문서)는 웹 브라우저가 화면을 그리기 위한 내용들이 많 이 들어가 있습니다. 이 중에서 우리가 필요한 단어나 내용은 아주 조금 입니다. 파싱은 html소스코드에서 필요한 내용만 뽑아내는 것입니다.

=>
위 그림 처럼 HTML 소스코드(문서)는 웹 브라우저가 화면을 그리기 위한 내용들이 많 이 들어가 있습니다. 이 중에서 우리가 필요한 단어나 내용은 아주 조금 입니다. 파싱은 html소스코드에서 필요한 내용만 뽑아내는 것입니다. 
------------------------------------------

106p
위 결과를 보면 맨 윗 줄에는 bs_obj 변수의 타입이 <class 'bs4.BeautifulSoup'> 뷰티풀솝4 이고 그 내용은 <html><div>hello</div></html>이고 여기에서 .find("div")를 했을 때 <div></div> 가 나온다는 뜻입니다.

=>
위 결과를 보면 맨 윗 줄에는 bs_obj 변수의 타입이 <class 'bs4.BeautifulSoup'> 뷰티풀솝4라는 뜻입니다. 그 내용은 두번째 줄에 있는 <html><div>hello</div></html>이고 여기에서 .find("div")를 했을 때 세번째 줄에 있는 <div></div> 가 나온다는 뜻입니다. 
------------------------------------------

110p
Hello 다음에 있는 bye를 뽑기 위해 인덱스를 사용해서 접근해보겠습니다.

=>
hello 다음에 있는 bye를 뽑기 위해 인덱스를 사용해서 접근해보겠습니다.
------------------------------------------

110p 하단
lis[1] 이 코드입니다 lis[0]을 쓰면 <li>hello</li>가 나옵니다. 첫 번째 있는 요소를 뽑으려면 0번을 쓰고 두 번째 요소를 뽑으려면 1번을 사용합니다.

=>
소스코드의 맨 마지막 줄의 print()안에 있는 lis[1] 입니다. lis[0]을 쓰면 <li>hello</li>가 나옵니다. 첫 번째 있는 요소를 뽑으려면 0번을 쓰고 두 번째 요소를 뽑으려면 1번을 사용합니다.
------------------------------------------

111p
print(lis[1].text) 이렇게 바꾸면 'bye'만 출력됩니다. 

=>
맨 마지막 줄을 print(lis[1].text) 이렇게 바꾸면 'bye'만 출력됩니다. 
------------------------------------------

111p 상단
결과
Bye

=>
결과
bye
------------------------------------------

111p 중단
먼저 태그에 대해서 간략히 알아보면 <ul></ul> <li></li> <div></div> <a></a>등 <>로 감 싸져 있는 걸 태그라고 합니다. <ul>로 시작 했으면 </ul>로 끝나는게 태그입니다.
많이 사용하는 태그로는 <ul></ul>, <li></li>, <div></div> <a>등이 있습니다.

속성(property)은 <ul class="greet"> 여기에서 class가 속성입니다. "greet"이 속성이 아닙니다. "greet"은 속성값 입니다. 이 둘을 잘 구분할 필요가 있습니다.

=>
먼저 태그에 대해서 간략히 알아보겠습니다. <ul></ul> <li></li> <div></div> <a></a>등 <>로 감 싸져 있는 것을 태그라고 합니다. <ul>로 시작 했으면 </ul>로 끝나는게 태그입니다.
많이 사용하는 태그로는 <ul></ul>, <li></li>, <div></div> <a>등이 있습니다.

속성(property)은 <ul class="greet"> 여기에서 class가 속성입니다. "greet"이 속성이 아닙니다. "greet"은 속성값 입니다. '속성'과 '속성값' 이 둘을 잘 구분할 필요가 있습니다.
------------------------------------------

111p 하단
그러면 이 a태그의 속성은 총 몇 개일까요? 이것도 꼭 위에 a태그를 보면서 꼭 생각해보시기 바랍니 다. 정답은 1개 입니다. Href 속성 하나뿐입니다.

=>
그러면 이 a태그의 속성은 총 몇 개일까요? 이것도 꼭 위에 a태그를 보면서 꼭 생각해보시기 바랍니 다. 정답은 1개 입니다. href 속성 하나뿐입니다.

수정한 내용:
href소문자로 수정함
------------------------------------------

114p
a태그에서 링크를 뽑아낼 때는 href라는 속성의 속성값을 뽑아내야 합니다. 태그, 속성, 속성값에 대 해서는 위에서 다루었으니 정확하게 모르신다면 한번 더 읽어보는걸 추천합니다.

=>
a태그에서 링크를 뽑아낼 때는 href라는 속성의 속성값을 뽑아내야 합니다. 태그, 속성, 속성값에 대 해서는 위에서 다루었으니 정확하게 모르신다면 한번 더 읽어보는 것을 추천합니다.
------------------------------------------

115p
이런 모양의 HTML이 있다고 합시다. 여기에서 네이버의 주소 https://www.naver.com을 뽑아내 려면 어떻게 해야 할까요?

=>
이런 모양의 HTML이 있다고 합시다. 여기에서 네이버의 주소 https://www.naver.com/을 뽑아내 려면 어떻게 해야 할까요?
------------------------------------------

116p
atag['href'] 이 코드입니다.

=>
atag['href']이 부분이 href속성의 속성 값을 뽑는 코드입니다.
------------------------------------------

### 5.4

117p
위 버튼에 있는 글자인 '네이버를 시작페이지로'라는 글자를 크롤링해보겠습니다.

=>

수정내용:
이 줄 지워주세요.
------------------------------------------

120p
영역을 마우스로 찾은 후에 클릭하면 앞의 화면처럼 HTML 소스코드에서 어떤 부분인지 표시를 해줍니다.

=>
개발자도구를 켠 상태에서 '네이버를 시작페이지로'버튼을 마우스로 찾은 후에 클릭을하면 앞의 화면처럼 HTML 소스코드에서 어떤 부분인지 표시를 해줍니다.
------------------------------------------

121p
이런 형태로 되어 있습니다. <a>라는 태그 안에 텍스트가 들어 있는 형태 입니다. ‘네이버를 시작페 이지로’옆에 있는 <span></span>은 여기에서는 중요하지 않습니다.

=>
<a>라는 태그 안에 텍스트가 들어 있는 형태 입니다. ‘네이버를 시작페 이지로’옆에 있는 <span></span>은 여기에서는 중요하지 않습니다.

------------------------------------------

121p
그러면 ▼기호로 바뀌면서 아래 내용이 열립니다. "네이버를 시작페이지로"라는 글자가 보입니다. 우리의 목표는 저 글자를 뽑아내는 겁니다.

=>
그러면 ▼기호로 바뀌면서 아래 내용이 열립니다. "네이버를 시작페이지로"라는 글자가 보입니다. 우리의 목표는 저 글자를 뽑아내는 것입니다.
------------------------------------------

121p 중간
위에 HTML코드를 조금 분석을 해보겠습니다.

=>
위 HTML코드를 분석 해보겠습니다.
------------------------------------------

121p 하단
Div 태그 안에 class="area_links" 이렇게 되어 있는 부분을 속성이라고 합니다. class=하고 시작 하므로 ' 클래스(class) 속성' 또는 그냥 '클래스' 라고 부릅니다. class="area_links"라고 되어 있는 부분이 중요합니 다. 이 클래스를 가지고 파이썬의 뷰티풀솝 라이브러리를 이용해 데이터를 뽑아낼 수 있기 때문입니다.

=>
div 태그 안에 class="area_links" 이렇게 되어 있는 부분을 속성이라고 합니다. class=하고 시작 하므로 ' 클래스(class) 속성' 또는 그냥 '클래스' 라고 부릅니다. class="area_links"라고 되어 있는 부분이 중요합니 다. 이 클래스를 가지고 파이썬의 뷰티풀솝 라이브러리를 이용해 데이터를 뽑아낼 수 있기 때문입니다.
------------------------------------------

123p
Div 태그 중에서 class가 "area_links"로 되어있는 div를 찾으라는 명령입니다.

=>
div 태그 중에서 class가 "area_links"로 되어있는 div를 찾으라는 명령입니다.
------------------------------------------

### 5.5

128p
<li>
	<a>
		<span></span>
		<span class=”an_txt”>메일</span>
</a> 
</li>

=>
<li>
	<a>
		<span></span>
		<span class=”an_txt”>메일</span>
	</a> 
</li>

수정한 내용:
들여쓰기 수정해주세요.
-------------------------------------------

129p
결과를 보면 맨 앞에 <ul class="an_l">로 시작합니다. 우리가 원하는 ul이 잘 뽑혔습니다.	

=>
결과를 보면 첫줄이 <ul class="an_l">로 시작합니다. 우리가 원하는 ul이 잘 뽑혔습니다.

수정한 내용:

--------------------------------------------

130p

print(ul ---> for li in ul:
	      print(li)

=>
print(ul) ---> for li in ul:
	      print(li)

수정한 내용:
)빠진 것 수정
--------------------------------------------










# 키워드

## 1장

파이썬
프로그래밍
코딩


## 2장
파이썬3
파이참
pycharm

2.3
프로젝트
project
디렉토리
폴더
앱
git
프로젝트 루트
프로젝트 익스플로러

2.4
패키지
package
모듈
소스코드

2.5
hello 

## 5장 
5.1
크롬
chrome
스크래핑

5.2
urllib
url
import
interpreter
웹브라우저
html
파싱
parsing
스크래핑
scraping
크롤링
crawling
크롤러
crawler


* 5.3
뷰티풀솝
beautifulsoup
urllib.request
urlopen
라이브러리
library
태그
tag
속성
property
속성값
인덱스
index
.find()
.findAll()
리스트
list
class
id
ul
li


5.4


5.5
[]
대괄호

